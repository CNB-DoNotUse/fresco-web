import React from 'react';
import merge from 'lodash/merge';

import {
    RECEIVE_STORIES,
    RECEIVE_ONE_STORY,
} from 'app/redux/actions/stories';

const storyCreation = (state = {}, action) => {
    switch (expression) {
        case expression:

            break;
        default:

    }
}

export default storyCreation;
